# Instrucoes de Python

Leia uma aula por vez, tente fazer o codigo sozinho e depois rode no terminal.

Para executar um arquivo Python, entre na pasta da atividade e use:

```powershell
python main.py
```

Se nao funcionar, tente:

```powershell
py main.py
```
